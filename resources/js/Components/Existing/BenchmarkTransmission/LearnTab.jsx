import LearnLayout from "@/Components/Common/LearnLayout";

export default function LearnTab() {
   // "benchmark_audit" slug audio/video pages par relevant content render karega
   return <LearnLayout slug="benchmark_audit" />;
}