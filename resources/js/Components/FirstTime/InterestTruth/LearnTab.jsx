import LearnLayout from "@/Components/Common/LearnLayout";

export default function LearnTab() {
   // "interest_truth" slug audio/video pages par relevant content render karega
   return <LearnLayout slug="interest_truth" />;
}